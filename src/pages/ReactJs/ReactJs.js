import React from 'react';
import withLayout from '../../layout/withLayout';

const ReactJs = () => {
  return (
    <main>
      <div>
        I am a junior React developer.
      </div>
    </main>
  );
};

export default withLayout(ReactJs);
